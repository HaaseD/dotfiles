# Replace default prefix (C-b) by C-a (like using screen)
set-option -g prefix C-a
unbind-key C-b
bind-key a send-prefix

# Tweak timing between key sequences
set -s escape-time 0

# Improve colors
set -g default-terminal 'screen-256color'

# Customize status line
set -g status-position top
set -g status-fg black
set -g status-bg green

# # set border styles
set -g pane-active-border-style fg=magenta
set -g pane-border-style fg=green
set -g pane-border-indicators colour
set -g pane-border-status off

# Use prefix + y for zooming
bind-key -T prefix y resize-pane -Z

# Config reload
bind r source ~/.tmux.conf\; display "~/.tmux.conf sourced!"

# Mouse support
# set -g mouse on

# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 1

# navigate panes as in vim
bind-key j select-pane -D
bind-key k select-pane -U
bind-key h select-pane -L
bind-key l select-pane -R

# Color inactive window
#set -g window-status-style bg=gray

# Color active window
#set -g window-status-current-style bg=yellow,fg=red
